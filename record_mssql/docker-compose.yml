
services:
  # qc_app:
  #   build:
  #     context: .  
  #     dockerfile: Dockerfile
  #   environment:
  #     - TZ=Asia/Bangkok
  #   image: "mic/qc_app:1.0.0"
  #   container_name: 'streamlit${NO}'
  #   privileged: true
  #   volumes:
  #     - ./telegraf.conf:/app/telegraf.conf
  #     - ./config.ini:/app/config.ini
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - ./.env:/app/.env
  #   ports:
  #     - '${ST_PORT}:8501'
  #   env_file:
  #     - .env
  #   command: ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]

  # qc_data:
  #   image: "mic/qc_app:1.0.0"
  #   container_name: 'qc_data${NO}'
  #   privileged: true
  #   volumes:
  #     - ./utils/.env:/app/utils/.env
  #     - ./.env:/app/.env
  #   env_file:
  #     - .env
  #   environment:
  #     - TZ=Asia/Bangkok
  #   command: ["python", "main_qc.py"]
    
  telegraf_mms:
    image: telegraf
    privileged: true
    container_name: 'telegraf_mms${NO}'
    restart: always
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf:ro
    ports:
      - '${TG_PORT}:8125'
    env_file:
      - .env

  influxdb_mms:
    image: influxdb:1.8-alpine
    container_name: 'influxdb_mms${NO}'
    restart: always
    environment:
      - INFLUXDB_DB=influx
      - INFLUXDB_ADMIN_USER=admin
      - INFLUXDB_ADMIN_PASSWORD=admin
      - TZ=Asia/Bangkok
    env_file:
      - .env
    ports:
      - '${INFLUX_PORT}:8086'
    volumes:
      # - '/home/influxdb_data${NO}:/var/lib/influxdb'
      - './influxdb_data${NO}:/var/lib/influxdb'

  # ofelia:
  #   image: mcuadros/ofelia:latest
  #   privileged: true
  #   container_name: 'ofelia${NO}'
  #   restart: always
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - ./config.ini:/etc/ofelia/config.ini
  #   env_file:
  #     - .env

  # grafana:
  #   image: grafana/grafana
  #   container_name: grafana
  #   restart: always
  #   ports:
  #     - 3000:3000
  #   environment:
  #     - GF_SECURITY_ADMIN_USER=admin
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana_data:/var/lib/grafana

volumes:
  # grafana_data:
  influxdb_data1:
    driver_opts:
      type: none
      # device: '/home/influxdb_data${NO}'
      device: './influxdb_data${NO}'
      o: bind


